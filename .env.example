# Token required to access the Field Station UI.
# Generate one: openssl rand -hex 32
# Leave blank (or omit) for no auth — suitable for local-only dev.
FIELD_STATION_TOKEN=

# Override the listen address. Default: 127.0.0.1:3457
# FIELD_STATION_ADDR=127.0.0.1:3457

# Override the Claude config directory. Default: ~/.claude
# CLAUDE_HOME=
